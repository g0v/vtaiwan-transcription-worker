{
	"name": "vtaiwan-transcription-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "npx wrangler deploy",
		"dev": "npx wrangler dev --remote",
		"dev:local": "npx wrangler dev",
		"start": "npx wrangler dev --remote",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"@cloudflare/workers-types": "^4.20250628.0",
		"@types/node": "^24.0.7",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.28.0"
	},
	"dependencies": {
		"chinese-conv": "^3.2.2"
	}
}
